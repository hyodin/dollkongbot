# ============================================================
# 프론트엔드 로컬 개발 환경 설정
# ============================================================
# 이 파일을 .env.local로 복사하여 실제 값을 입력하세요
# 사용법: cp env.local.example .env.local
# 주의: Vite는 VITE_ 접두사가 붙은 환경변수만 클라이언트에 노출됩니다

VITE_MODE=prod
# ============================================================
# API 서버 설정 (로컬 개발)
# ============================================================
# 백엔드 API의 Base URL
# 로컬 개발: Vite proxy를 통해 localhost:5000으로 요청
VITE_API_BASE_URL=/api/dollkongbot

# Vite 개발 서버 프록시 타겟 (선택사항)
# 백엔드가 다른 포트에서 실행되는 경우 수정
VITE_PROXY_TARGET=http://127.0.0.1:5000/

# ============================================================
# 네이버웍스 OAuth 설정 (로컬)
# ============================================================
# 네이버웍스 앱 등록 시 발급받은 Client ID
VITE_NAVERWORKS_CLIENT_ID=KG7nswiEUqq3499jB5Ih

# OAuth 리다이렉트 URI (로컬 개발용)
# 설정하지 않으면 자동으로 http://localhost:3005/dollkongbot/ 사용됨
VITE_NAVERWORKS_REDIRECT_URI=https://www.yncsmart.com/dollkongbot/


# OAuth 요청 스코프 (쉼표로 구분)
VITE_NAVERWORKS_SCOPE=user.read,mail,board.read

# ============================================================
# 인증 토큰 설정
# ============================================================
# 토큰 만료 전 갱신 시작 시간 (분 단위, 기본값: 2)
VITE_TOKEN_EXPIRY_BUFFER_MINUTES=2

# ============================================================
# 개발 모드 설정
# ============================================================
# 개발 모드 활성화 여부 (true/false)
VITE_DEV_MODE=true

