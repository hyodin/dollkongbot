# ============================================================
# 프론트엔드 운영 환경 설정
# ============================================================
# 이 파일을 .env.prod로 복사하여 실제 값을 입력하세요
# 사용법: cp env.production.example .env.prod (또는 .env.production)
# 주의: Vite는 VITE_ 접두사가 붙은 환경변수만 클라이언트에 노출됩니다

# ============================================================
# API 서버 설정 (운영)
# ============================================================
# 백엔드 API의 Base URL
# 운영 환경: 실제 도메인 URL 사용
VITE_API_BASE_URL=/api/dollkongbot

# 운영 환경에서는 프록시를 사용하지 않음
# VITE_PROXY_TARGET 설정 불필요

# ============================================================
# 네이버웍스 OAuth 설정 (운영)
# ============================================================
# 네이버웍스 앱 등록 시 발급받은 Client ID (운영용)
VITE_NAVERWORKS_CLIENT_ID=your_production_client_id_here

# OAuth 리다이렉트 URI (운영 환경)
# 설정하지 않으면 자동으로 https://www.yncsmart.com/dollkongbot/ 사용됨 (mode=prod)
# 다른 도메인을 사용하는 경우에만 수정하세요
# VITE_NAVERWORKS_REDIRECT_URI=https://www.yncsmart.com/dollkongbot/

# OAuth 요청 스코프 (쉼표로 구분)
VITE_NAVERWORKS_SCOPE=user.read,mail

# ============================================================
# 인증 토큰 설정
# ============================================================
# 토큰 만료 전 갱신 시작 시간 (분 단위, 기본값: 2)
VITE_TOKEN_EXPIRY_BUFFER_MINUTES=2

# ============================================================
# 개발 모드 설정
# ============================================================
# 개발 모드 활성화 여부 (true/false)
VITE_DEV_MODE=false

